<!DOCTYPE html>
<html lang="en">

<head>
  <title>{{siteTitle}}</title>
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="robots" content="noindex, nofollow" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="shortcut icon" type="image/x-icon" href="favicon.ico" />
  <link rel="alternate" type="application/rss+xml" title="{{siteTitle}}" href="{{feedHref}}" />
  <link href="index.css" rel="stylesheet" />
</head>

<body>
  <header>
    <dl>
      <dt><b>사이트 이름</b></dt><dd>{{siteTitle}}</dd>
      <dt><b>마지막 업데이트 시간</b></dt><dd>{{siteBuildTimestamp}}</dd>
      <dt><b>Cli 버전</b></dt><dd>{{cliVersion}}</dd>
      <dt><b>문서 URL</b></dt><dd>{{projectUrl}}</dd>
    </dl>
  </header>


  <ul>
  {{#each dates}}
    <li>
      <section>
      <h1>{{isoPublishDate}}</h1>

      <ul>
        {{#each sources}}
        <li>
          <section>
            <h2>
              <a href="{{siteUrl}}">{{title}}</a>
            </h2>

            {{#each articles}}
            <article>
                <h3><a href={{link}}>{{title}}</a></h3>
                <div>
                  <div>출처: <a href={{source.siteUrl}}>{{source.title}}</a></div>
                  <div>작성일: <time>{{isoPublishDate}}</time></div>
                  <div>작성 시간: <time>{{publishedOn}}</time></div>
                  <div>읽는 시간: <time>{{readingTimeInMin}}분</time></div>
                  <div>단어 수: <time>{{wordCount}}</time></div>
                </div>
              </details>
            </article>
            {{/each}}

          </section>
        </li>

        {{/each}}
      </ul>

      </section>
  {{/each}}
  </ul>
</body>

</html>
